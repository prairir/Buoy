package tun

import (
	"github.com/milosgajdos/tenus"
	"github.com/songgao/water"
	"net"
)

func Init(iname string, ip net.IP, network *net.IPNet) (*water.Interface, error) {
	thing := tenus.Link{}
	err := thing.SetLinkIp(ip, network)
	if err != nil {
		return err	}

	return nil, nil
}
